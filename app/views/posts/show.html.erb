<%= @post.title %>
<%= @post.body %>
<%= link_to "編集", edit_post_path(@post)%>
<%= link_to "削除", @post, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, status: :see_other, class: 'btn btn-danger'%>
<br>
<%= link_to "戻る", posts_path %>